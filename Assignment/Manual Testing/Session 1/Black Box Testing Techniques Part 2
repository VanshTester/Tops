Sure — here are the answers in a **simple, human-written assignment style**, with clear test cases.

### 1. Statement Coverage

The code has two possible statements: `return 0` and `return 50`.
To get **100% statement coverage**, we need to execute both statements at least once.

| Test Case | Input: amount | Expected Output | Statement Covered |
| --------- | ------------: | --------------: | ----------------- |
| TC1       |          ₹500 |               0 | `return 0`        |
| TC2       |          ₹300 |              50 | `return 50`       |

**Result:** Both return statements are executed, so **100% statement coverage** is achieved.

---

### 2. Branch Coverage

There are two decisions:

* `userType == "student"`
* `total > 1000` (only checked when the user is a student)

To cover all branches, we need to test both **True and False** outcomes.

| Test Case | User Type | Total | Expected Output | Branch Covered                       |
| --------- | --------- | ----: | --------------: | ------------------------------------ |
| TC1       | student   | ₹1500 |              20 | Student = True, Total > 1000 = True  |
| TC2       | student   |  ₹800 |              10 | Student = True, Total > 1000 = False |
| TC3       | regular   | ₹1500 |               5 | Student = False                      |

**Result:** All possible branches are executed, so **100% branch coverage** is achieved.

---

### 3. Condition Coverage

The condition is:

`(method == "UPI" OR method == "Card") AND amount <= 10000`

There are three basic conditions:

* C1: `method == "UPI"`
* C2: `method == "Card"`
* C3: `amount <= 10000`

For condition coverage, each condition should become **True and False** at least once.

| Test Case | Method | Amount | C1 UPI | C2 Card | C3 Amount ≤ ₹10,000 | Expected |
| --------- | ------ | -----: | ------ | ------- | ------------------- | -------- |
| TC1       | UPI    |  ₹5000 | T      | F       | T                   | True     |
| TC2       | Card   |  ₹5000 | F      | T       | T                   | True     |
| TC3       | Cash   |  ₹5000 | F      | F       | T                   | False    |
| TC4       | UPI    | ₹12000 | T      | F       | F                   | False    |

**Result:** Every condition has been tested as both **True and False**, so **full condition coverage** is achieved.

---

### 4. Path Coverage

There are **3 unique execution paths** in this login function:

1. Username is not admin → User Not Found
2. Username is admin + correct password → Login Success
3. Username is admin + wrong password → Wrong Password

| Test Case | Username | Password | Expected Output | Path                                 |
| --------- | -------- | -------- | --------------- | ------------------------------------ |
| TC1       | user1    | pass123  | User Not Found  | Username ≠ admin                     |
| TC2       | admin    | pass123  | Login Success   | Username = admin, Password correct   |
| TC3       | admin    | wrong123 | Wrong Password  | Username = admin, Password incorrect |

**Result:** All **3 unique execution paths** are covered, so **100% path coverage** is achieved.
